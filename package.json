{
  "name": "magic-frame-free-v5",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/prepare-assets.mjs && vite",
    "build": "node scripts/prepare-assets.mjs && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.35"
  },
  "devDependencies": {
    "vite": "6.1.0"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
